# 🚀 Quick Start — 약 20분 셋업

> 자세한 설명·문제 해결은 [README.md](README.md) 참고. 이 페이지는 **막힘없이 진행했을 때의 최단 경로**만 담았습니다.

## ✅ 시작 전 확인 (1분)

다음 세 계정 모두 데스크톱 브라우저에서 로그인할 수 있어야 합니다:

- [ ] **키즈노트** ([www.kidsnote.com](https://www.kidsnote.com)) — 어린이집에서 받은 평소 계정 (아이디·비밀번호로 로그인)
- [ ] **노션** ([www.notion.so](https://www.notion.so)) — 없으면 1분 가입
- [ ] **GitHub** ([github.com](https://github.com/signup)) — 없으면 1분 가입

> 📱 휴대폰만으로는 셋업할 수 없습니다. 데스크톱·노트북이 필요해요.

> 👨‍👩‍👧‍👦 **자녀가 2명 이상이면** 아래를 첫째 기준으로 끝낸 뒤 [README의 A. 자녀가 여러 명일 때](README.md#a-자녀가-여러-명일-때)에서 페이지 하나와 시크릿 2개만 더 추가하세요. Fork는 하나면 됩니다.

---

## 1️⃣ Fork (1분)

**Fork** = 이 프로젝트를 내 GitHub 계정으로 복사하는 것.

[github.com/redchupa/kidsnote-backup](https://github.com/redchupa/kidsnote-backup) 페이지 우측 상단, `Star` 옆의 **`Fork`** → 다음 화면에서 녹색 **`Create fork`** 클릭.

✅ 주소가 `내깃허브아이디/kidsnote-backup`인 내 사본 페이지로 이동되면 성공.

---

## 2️⃣ 노션에 빈 페이지 만들기 (1분)

> 💡 노션은 화면이 자주 바뀝니다. 버튼 이름이 조금 다르면 비슷한 이름을 찾아 누르세요.

1. 노션 좌측 사이드바 맨 위 **📝** (새 페이지) 클릭 — 또는 `개인 페이지` 글자에 마우스를 올리면 나오는 **`+`**
2. 제목에 **`키즈노트 백업`** 입력, 본문은 비워둠

✅ 사이드바에 `키즈노트 백업` 페이지가 보이면 성공. (표는 첫 백업 때 자동으로 생깁니다)

---

## 3️⃣ 노션 연결 만들고 토큰 받기 (3분)

**연결(Connection)** = 이 도구가 내 노션에 글을 쓸 수 있게 하는 출입증 (예전 이름: 통합/Integration).

1. [app.notion.com/developers/connections](https://app.notion.com/developers/connections) 접속
2. **`Create a new connection`** 클릭
3. 이름 `kidsnote-backup`, 본인 워크스페이스 선택 → **`Save`**
4. **`Configuration`** 탭에서 콘텐츠 권한 읽기·업데이트·입력(Read / Update / Insert content)이 모두 체크됐는지 확인
5. 같은 탭의 **`Installation access token`** 복사 → 메모장에 붙여두기

✅ 메모장에 `ntn_` (예전 토큰은 `secret_`) 으로 시작하는 토큰이 있으면 성공.

---

## 4️⃣ 페이지에 연결 추가 + 링크 복사 (2분)

1. `키즈노트 백업` 페이지 열기 → 우측 상단 **`•••`** → **`연결`** (Connections) → **`+ 연결 추가`** (Add connection)
2. `kidsnote-backup` 검색 → 클릭 → 접근 허용 확인
3. 우측 상단 **`공유`** (Share) → **`링크 복사`** (Copy link) → 메모장에 붙여두기 (브라우저 주소창 주소를 복사해도 됨)

✅ 메모장에 **토큰**과 **페이지 링크** 두 개가 있으면 성공. 링크는 `www.notion.so` 또는 `app.notion.com` 어느 쪽이든 그대로 쓰면 됩니다.

---

## 5️⃣ 키즈노트 아이디·비밀번호 확인 (1분)

따로 할 일은 없습니다. [www.kidsnote.com](https://www.kidsnote.com)에 로그인할 때 쓰는 **아이디·비밀번호**를 6번에서 그대로 입력합니다.

✅ 그 아이디·비밀번호로 키즈노트 웹 로그인이 되면 성공.

---

## 6️⃣ GitHub 시크릿 6개 등록 (5분)

**시크릿** = GitHub에 암호화해서 보관하는 비밀 값.

1. 내 fork 페이지 (`https://github.com/내깃허브아이디/kidsnote-backup`) → 위쪽 메뉴 줄 **`Settings`**
   - ⚠️ 우측 상단 프로필 사진 메뉴의 Settings가 아닌 **메뉴 줄의 Settings**
2. 왼쪽 사이드바 **`Security`** 묶음의 **`Secrets and variables` → `Actions`**
3. **`New repository secret`** → `Name`과 `Secret` 입력 → **`Add secret`**. 이걸 **6번** 반복:

| Name (표와 똑같이) | Secret 값 |
|---|---|
| `NOTION_TOKEN` | 3번에서 복사한 토큰 |
| `NOTION_DATABASE_ID` | 4번에서 복사한 **페이지 링크 통째로** |
| `KIDSNOTE_USERNAME` | 키즈노트 **아이디** |
| `KIDSNOTE_PASSWORD` | 키즈노트 **비밀번호** |
| `KIDSNOTE_CHILD_NAME` | 백업할 자녀 이름 (예: `우하린`, 일부만 적어도 됨). 자녀가 1명이어도 등록 |
| `AI_FEATURES` | **`off` 권장** — 아래 안내 |

✅ 시크릿 목록에 위 6개 이름이 정확히 보이면 성공 (값은 안 보이는 게 정상).

> 🔒 **비밀번호도 안전합니다**: 시크릿은 암호화되어 저장되고, 등록 후에는 본인도 값을 다시 볼 수 없으며, 실행 로그에 나오지 않고, 다른 누구에게도 보이지 않습니다.

> ⚠️ **이름은 정확히**, 값은 대충 붙여넣어도 OK (앞뒤 공백·줄바꿈·따옴표는 자동 정리, `on`/`off` 대소문자 무관). 이름을 틀렸으면 수정이 안 되니 휴지통 아이콘으로 삭제 후 다시 추가하세요.

> 🤖 **`AI_FEATURES`는 `off` 권장**:
> - **`off`** (권장) → 빠른 백업 (1년치 1-3시간). 알림장·사진·식단 + 통계 대시보드 2개.
> - **`on`** → 자녀 일기/부모 편지/AI 대시보드 4종이 추가되지만 **무료 로컬 LLM 품질이 완벽하지 않고 1년치 5-15시간 소요**.
>
> 나중에 마음이 바뀌면 시크릿 값만 바꾸면 됩니다.

---

## 7️⃣ 자동 실행 켜고 백업 시작 (클릭 3분 + 자동 진행)

1. fork 페이지 위쪽 **`Actions`** 탭 → 처음이면 녹색 **`I understand my workflows, go ahead and enable them`** 클릭
2. 왼쪽 목록에서 **`Kidsnote → Notion mirror`** 클릭 (`개발자용 테스트 (사용자는 실행할 필요 없음)`은 무시)
3. 예약 실행이 꺼져 있다는 안내와 **`Enable workflow`** 버튼이 보이면 **꼭 클릭** — 이래야 6시간마다 자동으로 돕니다
4. **첫 테스트**: 오른쪽 **`Run workflow ▾`** → 브랜치 `main` 그대로, `limit`에 **`3`** 입력, 나머지(`monthly_sample`, `force_refresh`, `relabel`)는 `off` 그대로 → 녹색 **`Run workflow`**
5. **확인**: 새 실행 클릭 → 왼쪽 **`백업 (자녀 1)`** → **`Check login and settings`** 펼치기
   - 노란색/돌아가는 아이콘 = 실행 중 (실패 아님), 녹색 체크 = 성공, 빨간 X = 실패
   - 성공이면 `키즈노트 로그인 OK`, `자녀 확인 OK: 우*린`, `노션 연결 OK` 줄이 보임
   - `백업 준비 확인 실패` 안내가 나오면 끝의 `[이유 코드]`를 [README의 이유별 해결 표](README.md#준비-확인-실패-이유별-해결)에서 찾기
6. 몇 분 후 노션 `키즈노트 백업` 페이지 확인 — 안에 표가 생기고 알림장 3건 + 사진이 들어와 있으면 성공
7. **전체 백업**: 다시 `Run workflow` → 이번엔 `limit` **비워둠** → 녹색 `Run workflow`

> 📧 **실패 메일 받기 확인 (한 번만)**: GitHub 프로필 사진 → `Settings` → `Notifications` → `System`의 `Actions`를 Email + "Only notify for failed workflows"로. ([자세히](README.md#7-6-실패-알림-메일-설정-확인-한-번만))

> 🤖 **이후로는 자동**. 6시간마다 새 알림장을 백업하고, 한 번에 다 못 끝낸 백업도 다음 자동 실행 때 이어서 진행합니다.

---

## 🎉 셋업 완료

이제 **컴퓨터를 꺼도** GitHub의 클라우드 서버가 알아서 백업합니다. 첫 백업이 끝나면 노션에 알림장·사진·댓글·식단·앨범·공지 + 통계 대시보드가 모두 정리돼요 (AI를 켰으면 자녀 일기/부모 편지/AI 대시보드 4종도). 이후 새 알림장은 6시간 안에 자동 추가.

---

## 📚 더 알아보기

다음 경우에는 [README.md](README.md)를 참고하세요:

- **자녀가 2명 이상** → [A. 자녀가 여러 명일 때](README.md#a-자녀가-여러-명일-때)
- **AI 가공을 켜고/끄거나 일부만 끄고 싶음** → [B. AI 가공 켜기/끄기](README.md#b-ai-가공을-안-쓰고-단순-백업-통계만-원할-때)
- **어디서 막혔음 / 에러 발생** → [문제 해결](README.md#문제-해결-자주-나오는-에러)
- **`백업 준비 확인 실패` 메일이 왔음** → [백업 준비 확인 실패 메일이 왔을 때](README.md#백업-준비-확인-실패-메일이-왔을-때)
- **셋업 이후 할 일 / 코드 업데이트 받기** → [두 번째 이후 백업](README.md#두-번째-이후-백업-거의-안-해도-됨), [Sync fork](README.md#코드-업데이트가-있을-때-내-fork-동기화)
- **노션 페이지를 웹에 공개하고 싶음** → [(선택) 노션 페이지 웹 공개](README.md#선택-노션-페이지를-웹에-공개하기)
- **자주 묻는 질문** → [자주 묻는 질문](README.md#자주-묻는-질문)
